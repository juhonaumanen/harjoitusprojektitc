#include <iostream> // this is a preprosessor that calls iostream elements and commands
#include <string> // this includes string functionality

void(); //this is a void function, a function without a return value
int main(); //
{
    //this is the main function body, a part that all executables need
    return 0; // return 0 tells that the program is done and returns to the start. 0 is successs 1 is error.
}