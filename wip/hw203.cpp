#include <iostream>

float price(int iAge)
{
    int iSenior = 65;   //palauttaa vanhuksen hinnan
    int iChild = 16;    //palauttaa lapsen hinnan
    bool iStudent;
}

    if (iAge => iSenior)
        {
            return 2.40 eur;
        }
    if (iAge <= iChild)
        {
            return 2.40 eur;
        }
    else
        {
            return 3.80 eur;
    }

int main()
{
    std::cout << "your age? "<< "\n"
    int iAge;
    std::cin >> int iAge >> "\n"

    char yorn;
    std::cout "student? y/n" >> yorn"\n"
    std::cin  >> yorn"\n"
    if(yorn='y'){
        return iStudent == true;
    }
    else()
    {
         return iStudent == false;
    }
    std::cout  << "your admission fee is " << int iSenior << "\n"
return 0;
}