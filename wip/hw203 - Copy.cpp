#include <iostream>

int age;
int adult (age >= 16 && < 65);
bool student;
int child (int age <16);
int senior(int age >=65);
bool isStudent();{
   if (int student == y){
       return true;
            if (student = n)
            return false;
   }
}


int main(){

std::cin >> "type your age " >> age >> "\n";
std::cin >> " are you a student? yes/no" >> student >> "\n";

    if(age == adult, isStudent== false)
        {
            std::cout << "your admission fee is 3,80 €" << std::endl;
        }
    if(age == adult, isStudent == true)
        {
            std::cout << "your admission fee is 2,40€ " << std::endl;
        }
    if(age == child)
        {
            std::cout << "your admission fee is 2,40€ " << std::endl;
        }
    if (age == senior)
        {
            std::cout << "your admission fee is 2,40€ " << std::endl;
        }

return 0;
}